'use strict';

/**
 * 테스트케이스 종류의 이름과 색 — **브라우저 쪽의 유일한 정의처**.
 *
 * 이 값들은 표(dashboard.js)와 인쇄 문서(report.js) 양쪽에 필요하다. 빌드가 없는
 * 구조라 각 파일에 따로 적어 뒀더니 두 가지가 터졌다.
 *   · 클래식 `<script>` 는 전역을 공유해서 `const` 를 두 번 선언하면 페이지가 죽는다
 *   · 죽지 않더라도 한쪽만 고치면 표와 PDF 의 이름이 갈라진다
 * 그래서 제일 먼저 읽히는 이 파일에 한 번만 둔다. 서버(src/csv.js)와 값이 같은지는
 * 테스트가 본다.
 *
 * 이름을 고를 때의 규칙: **수행 결과로 읽힐 수 있는 단어를 쓰지 않는다.**
 * `Pass`/`Fail` 도, 그 번역인 `정상`/`실패` 도 QA 가 결과를 적을 때 쓰는 말이라
 * "아직 안 돌렸는데 왜 결과가 적혀 있지?" 라는 오해를 그대로 남겼다.
 */
const TYPE_LABEL = {
  Pass: '기능 확인',
  Fail: '오류 처리',
  'Edge Case': '경계값',
};

/** 화면 표의 알약 색 */
const TYPE_CLASS = { Pass: 'pass', Fail: 'fail', 'Edge Case': 'edge' };

/** 인쇄 문서의 배지 색 */
const TYPE_BADGE = { Pass: 'pass', Fail: 'fail', 'Edge Case': 'edge' };

/** 이 칸이 무엇인지 — 표 머리와 알약의 툴팁에 함께 쓴다 */
const TYPE_HINT = '어떤 종류의 케이스인지 — 수행 결과가 아닙니다';
